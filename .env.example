# YouTube Data API v3 Key
# Get your API key from: https://console.developers.google.com/
# spotify API support dropped due to bad compatibility with project
# we are essentially performing the outsourcing of our ML logic to spotify. 
# YouTube API
YOUTUBE_API_KEY=

# Databases
SQLITE_DATABASE_URL=sqlite:///app.db
# Render Postgres internal URL, e.g.
# POSTGRES_DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME
POSTGRES_DATABASE_URL=

# Backward-compat (optional): if set to a Postgres URL, used as Postgres
# used if needed for live multi user project(needs dedicated postgressDB server)
# DATABASE_URL=

# Read preference: postgres or sqlite
DB_READ_PREFERENCE=postgres

# Redis (Render internal URL), e.g. redis://default:PASSWORD@HOST:PORT
REDIS_URL=
REDIS_TTL_SECONDS=3600
